// export function base64decoder(input) {...}

export const base64decoder = (input) =>  {
  console.log('input=', input);
  // error with base64decoder code; String is not encoded correctly.
  return '********';
}
